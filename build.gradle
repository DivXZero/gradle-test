
apply plugin: 'cpp'

model {
  components {
    main(NativeExecutableSpec) {
      sources{
        cpp {
          source {
            srcDir "cpp/"
          }
        }
      }
    }
  }
}

task wrapper(type: Wrapper) {
  gradleVersion = '2.3'
}
